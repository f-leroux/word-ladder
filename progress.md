Original prompt: ok nice! Can you explain in more detail your setup for how you're able to serve the app and visually inspect it? I'm curious. Also, I'd say we need to have some UI changes. For me, the start word should be at the top, the destination word at the bottom (it should be something like you see at the top the first word, under that all the words you entered, then some kind of ellipses or something and then you see the destination word), the words you enter should be perfectly aligned with the initial word (you see now they're more to the left), and it should be more like, the new word you're entering is already filled in with the previous word and you can only change one letter, I'm not sure exactly what the best UI should be for this but like maybe you have the word already filled in and you see a cursor on the first letter to indicate that you can change it, and you can either use arrows or space bar and backspace or click to switch to another letter that you might want to change

- 2026-03-11: Traced offline puzzle generation and verified current UI via local static serve plus headless Chrome screenshot.
- 2026-03-11: Next step is to redesign the ladder layout so start is on top, finish is on bottom, input aligns with rows, and editing starts from the previous word with a single editable letter at a time.
- 2026-03-11: Implemented a draft-row editor that starts from the previous word, keeps exactly one changed letter at a time, supports click plus keyboard navigation, and moves submit below the row to keep the ladder centered.
- 2026-03-11: Rebuilt with Node 20, served the production bundle locally, and visually confirmed the new top-to-bottom ladder layout in a headless Chrome screenshot.
- 2026-03-11: Added light/dark theming, subtler edited-letter styling, right-side input controls, and dual-frontier solving so the active entry point can switch between the start and end words.
- 2026-03-11: Verified the latest layout in a browser screenshot; light mode rendered by default from system preference and the draft row remained aligned after moving controls to the right.
- 2026-03-11: Switched the side-toggle shortcut from Escape to Tab, mirrored the end-side editor so the draft sits directly above the active bottom frontier, and moved START/END labels to the left.
- 2026-03-11: Verified both the default top-down layout and the mirrored end-building layout in headless Chrome screenshots, then removed the temporary debug page used for the end-mode check.
- 2026-03-11: Reworked row actions so Submit is anchored to the active draft row and Build From Start/End is anchored to the opposite frontier row, removed the extra target-row wrapper styling, and fixed label alignment with a fixed left label column.
- 2026-03-11: Added a capture-phase global Tab handler plus removed game/header buttons from the tab order so side switching should trigger immediately instead of walking through focusable elements first.
- 2026-03-11: Reverified the default layout and two mirrored end-writing states in headless Chrome screenshots, then deleted the temporary debug pages.
- 2026-03-11: Added keyboard hints inside the Submit and Build buttons, moved the bottom-side error message above the draft row so mirrored spacing stays symmetrical, and widened the action buttons to keep each label on one line.
- 2026-03-11: Reserved a hidden draft-row slot on the inactive side of the ladder so switching between top and bottom entry keeps the start and end frontier rows locked to the same vertical positions.
- 2026-03-11: Removed the last layout-affecting validation slot by overlaying error text relative to the active draft row instead of allocating flow space, which keeps the central gap stable in both entry modes.
- 2026-03-11: Removed the remaining top-entry-only input offset and nudged the end-mode separator upward, then rebuilt and rechecked both modes in headless Chrome. Playwright is still unavailable locally, so browser verification remained screenshot-based.
- 2026-03-11: Made separator placement explicit for both modes so it sits lower while building from the start and higher while building from the end, then rebuilt and rechecked both states in headless Chrome.
- 2026-03-11: Removed the unrequested Undo button from the UI and changed separator placement to a fixed-height slot with a direct glyph offset so the start/end bias is explicit instead of relying on subtle container transforms.
- 2026-03-11: Corrected the separator placement by changing render order instead of nudging CSS; it now renders below the hidden spacer when building from the start and above it when building from the end, which matches the intended top/bottom behavior.
- 2026-03-11: Rejected the first French source because the accent-stripped five-letter set was too small. Switched to a fuller MIT French list with accents preserved, confirmed 6,806 five-letter entries and enough graph connectivity for 1,000 puzzles, then generated `src/data/fr-words.json` and `src/data/fr-puzzles.json`.
- 2026-03-11: Added URL-driven locale selection (`?lang=fr`), locale-specific storage/content loading, French UI/share strings, and accented French input/alphabet handling. Verified English and French screenshots in headless Chrome, plus an explicit accented French state before the final header-only CSS tweak.
- 2026-03-11: Switched French back to accentless gameplay by normalizing the source dictionary/common-word list/puzzles, reducing the runtime French alphabet to plain `a-z`, and updating help copy so accents are ignored instead of required. The normalized French graph now has 5,891 unique five-letter words and still yields 1,000 puzzles.
- 2026-03-11: `npm run build` passed under Node 20 after the accentless French change. The Playwright client is still blocked locally because the `playwright` package is not installed, so verification stayed on the established static-serve + headless Chrome screenshot loop. Confirmed English still shows `FILES → POLAR` and French now shows accentless `LAIDE → VERRE` under the `L'Échelle` title.
- 2026-03-11: Added GitHub Pages deployment via `.github/workflows/deploy.yml`, switched Vite to a dynamic `base` that derives the correct project-site path inside GitHub Actions, and replaced the template README with project-specific local-development, data-generation, and deployment documentation. `npm run build` still passes; the only remaining build warning is the existing large JS chunk from eager locale data loading.
- 2026-03-12: Added a side-aware undo action beside the current frontier word plus an `Esc` shortcut, localized the button/help text, and verified both the top-entry and end-entry undo layouts in headless Chrome screenshots after seeding temporary localStorage state. Playwright is still unavailable locally because the `playwright` package is missing.
- 2026-03-12: Fixed two undo edge cases: focused input cells now intercept `Esc` so undo fires on the first keypress instead of after focus is cleared, and undo now recomputes `isChainsConnected(nextState)` so removing a word can immediately complete the puzzle when the frontiers become adjacent again. Rebuilt successfully and rechecked the first-press `Esc` behavior in headless Chrome with a temporary same-origin test harness.
- 2026-03-12: Hardened the `Esc` path again by moving undo to input capture phase and making the document-level handler ignore focused text inputs, which prevents the browser’s selection-clearing behavior from eating the first press. Added per-step change arrows and live frontier-only shared-letter highlighting (green) on the current top and bottom frontier rows, including the draft row on the active side. Rebuilt successfully and verified start-side, end-side, and single-press-`Esc`-to-solved states in headless Chrome with a temporary debug page.
- 2026-03-12: Switched the undo shortcut from `Esc` to standalone `Shift`, implemented as a keydown/keyup pair that cancels itself if any other key is pressed in between, so `Shift+letter` still types normally. Also thickened the change arrows, removed the old changed-cell background fill, made the frontier-match green much stronger, and gave the active draft row a distinct orange border so the current word is easier to distinguish. Rebuilt successfully and verified start-side, end-side, and standalone-`Shift`-to-solved states in headless Chrome with a temporary debug page.
- 2026-03-12: Fixed the mirrored bottom-entry arrow so draft-row change arrows render below the edited cell instead of above it, simplified the start/end rows to use normal cell borders, and refined the arrow shape again. Rebuilt successfully and verified both the neutral start/end state and the edited bottom-draft state in headless Chrome before preparing the push.
- 2026-03-12: Added a mobile-only input dock with a custom on-screen keyboard, a top action row for left/right movement, submit, undo, and side switching, and a locale-aware layout rule that uses QWERTY by default and AZERTY for French/browser-French users. Kept the desktop layout unchanged by scoping the new dock to the mobile breakpoint, then rebuilt successfully and verified the current desktop render plus mobile English and mobile French screenshots in headless Chrome.
